/**
 * Theme toggle placeholder.
 * Future-proofed for dark mode support.
 */

export function initTheme() {
  // Dark mode can be enabled later by:
  // 1. Adding a toggle button to the nav
  // 2. Setting data-theme attribute on <html>
  // 3. Updating CSS custom properties in tokens.css
  //
  // For now, the site uses a single light theme.
}
