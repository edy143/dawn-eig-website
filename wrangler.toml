# Cloudflare Pages configuration
# This project uses Pages Functions (functions/ directory)
# Deploy via: npx wrangler pages deploy .
# Local dev:  npx wrangler pages dev .

name = "dawn-eig-website"
compatibility_date = "2024-01-01"

[vars]
# Public vars can go here; secrets (RESEND_API_KEY, CONTACT_EMAIL) must be set
# in the Cloudflare Dashboard → Pages → Settings → Environment variables
